const valume = x => x * x *x;
//const area = x => x*x;
// es 모듈에서는 export default 전체로 바뀜

// export default {
//   valume, //shorthand property : 키 밸류 동일할때 생략가능함
//   area
// };

export default valume;